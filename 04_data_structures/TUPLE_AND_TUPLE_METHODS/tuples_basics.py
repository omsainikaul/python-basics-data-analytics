numbers = (1, 4, 6)

print(numbers[0])
#print value from position 0 from tuple(numbers)

single = (1, )
#for single value tuple we must apply comma after the first value
print(single)

#tuple is immutable
#means tuple values can not be changed after creation of the tuple
